#include <cstring>
#include <iostream>
#include <algorithm>
using namespace std;
int temp;
int main()
{
    char B[92],A[92],C[92];
    cin >> B;
    sort(B, B+strlen(B));
    for (int i = 0; i < strlen(B); i++)
        if(B[i]) == 0;
            B[i] =

}
