#include <bits/stdc++.h>
#define endl '\n'
using namespace std;

int n, m, p, t;

int main()
{
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	
	cin>>n>>m;

	if(m % 2 == 0 || n % 2 == 0) 
	{
		cout<<-1<<endl;
		return 0;
	}

	cout<<32<<endl;
	return 0;
}
