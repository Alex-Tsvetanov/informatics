#include <cstdio>
#include <iostream>
using namespace std;

int main() {
    int n, a, b;
    int p, q;
    cin >> n >> a >> b;
    scanf("%d%d%d", &n, &a, &b);

    while (cin >> p >> q) {

    }

    cout << "X\n";
    return 0;
}
