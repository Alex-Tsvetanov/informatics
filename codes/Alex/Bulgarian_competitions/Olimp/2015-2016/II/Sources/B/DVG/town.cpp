#include<iostream>
using namespace std;
int main()
{
	bool  number, x;
	long int n, p[100000], q[100000], a, b,B=0;
	do
	{
		cin >> n >> a >> b;
	} while (n <= 1 || n >= 200000);
	for (int i = 0; i < n; i++)
	{
		cin >> p[i] >> q[i];
	}
	for (int i = 0; i < n; i++)
	{
		if (true)
		{
			number = true;
		}
	}
	if (number)
	{
		cout << 1 << "\n";
	}
	return 0;
}