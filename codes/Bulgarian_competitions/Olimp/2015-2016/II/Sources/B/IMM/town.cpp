#include<iostream>
using namespace std;
int main()
{
	long int n, a, b, p[100000], q[100000];
	do
	{
		cin >> n >> a >> b;
	} while (n <= 1 || n >= 200000);
	for (int i = 0; i < n; i++)
	{ 
		cin >> p[i] >> q[i];
	}
	for (int i = 1; i < n;i++)
	{ 
		if ()

		return 0;
}
	
