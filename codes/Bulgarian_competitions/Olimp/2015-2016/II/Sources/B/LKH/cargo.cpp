#include <iostream>

using namespace std;

int main()
{
    int N,M,P,i;
    do{cin>>N;}
    while(N<7 || N>124);
    do{cin>>M;}
    while(M<1 || M>1000);
    do{cin>>M;}
    while(P<1 || P>2000);
    int S[M];
    int E[M];
    while(i<M)
    {
        cin>>S[i];
        cin>>M{i};
        i++;
    }
    cout<<"IMPOSSIBLE";

    return 0;
}
